# `@react-native-ui-components/eslint-config`

## Usage

Install:

```bash
$ yarn add --dev @react-native-ui-components/eslint-config
```

Edit `package.json`:

```jsonc
{
	// ...
	"eslintConfig": {
		"extends": "@react-native-ui-components/eslint-config"
	}
}
```
